<?xml version="1.0" encoding="utf-8"?>
<org.coffeetrain.doggifs.SettingsScreenView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/settings_screen"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/app_background"
    android:orientation="vertical"
    >
  <org.coffeetrain.doggifs.ActionBarView
      android:id="@+id/action_bar"
      android:layout_width="match_parent"
      android:layout_height="@dimen/action_bar_height"
      />
  <LinearLayout
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:orientation="vertical"
      >
    <TextView
        android:id="@+id/set_available_gifs"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/set_available_gifs"
        style="@style/SettingsRow"
        />
    <TextView
        android:id="@+id/download_more_gifs"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:enabled="false"
        android:text="@string/download_more_gifs"
        style="@style/SettingsRow"
        />
    <TextView
        android:id="@+id/cycle_settings"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:enabled="false"
        android:text="@string/cycle_settings"
        style="@style/SettingsRow"
        />
  </LinearLayout>
</org.coffeetrain.doggifs.SettingsScreenView>

